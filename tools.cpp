//#include "tools.h"


void makeDataBaseC()
{
/*    if (!dbSql.connect("controller",
                       MrwSettings::instance()->dataBaseControllerHost,
                       MrwSettings::instance()->dataBaseControllerLogin,
                       MrwSettings::instance()->dataBaseControllerPassword)) return;
    char c=getch();
    if (c=='y')
    {
        dbSql.makeDBOnController();
        printf("Make DB\n");
    }
    dbSql.disconnect();*/
}

void makeDataBaseS()
{
/*    if (!dbSql.connect("server",
                       MrwSettings::instance()->dataBaseServerHost,
                       MrwSettings::instance()->dataBaseServerLogin,
                       MrwSettings::instance()->dataBaseServerPassword)) return;
    char c=getch();
    if (c=='y')
    {
        dbSql.makeDBOnServer();
        printf("Make DB\n");
    }
    dbSql.disconnect();*/
}

void formatCard()
{
/*    mRsReader.open();

    Iso4kbBur card(&mRsReader, 2);
    if (!card.format()) qDebug("Error format");
        else qDebug("Format is fine");*/
}

